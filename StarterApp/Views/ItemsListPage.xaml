<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="StarterApp.Views.ItemsListPage"
    Title="Marketplace">

    <VerticalStackLayout Padding="20" Spacing="15">

        <Label
            Text="Available Items"
            FontSize="30"
            FontAttributes="Bold"
            HorizontalOptions="Center"/>

        <Button
            Text="Load Items"
            Padding="15"
            CornerRadius="10"
            Command="{Binding LoadItemsCommand}" />
        <Button
            Text="Create New Item"
            Padding="15"
            CornerRadius="10"
            Clicked="GoToCreateItemPage" />
        <Button
            Text="View Rentals"
            Padding="15"
            CornerRadius="10"
            Clicked="GoToRentalsPage" />

        <CollectionView ItemsSource="{Binding Items}">

            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <Frame
                        Padding="15"
                        Margin="0,10"
                        BorderColor="LightGray">

                        <VerticalStackLayout Spacing="10">

                            <Label
                                Text="{Binding Title}"
                                FontSize="20"
                                FontAttributes="Bold"/>

                            <Label
                                Text="{Binding Description}" />

                            <Label
                                Text="{Binding DailyRate, StringFormat='£{0}/day'}"
                                FontAttributes="Bold"
                                TextColor="Green" />

                            <Button
                                Text="Request Rental"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RequestRentalCommand}"
                                CommandParameter="{Binding .}" />

                        </VerticalStackLayout>

                    </Frame>

                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </VerticalStackLayout>

</ContentPage>